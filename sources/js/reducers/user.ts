/**
 * Inital state
 */

const initialState = {
  isAuthenticated: false,
};

/**
 * Expo
 */

export default (state = initialState, action: Action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
