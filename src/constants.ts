import { Color } from "three";

export const ROOT_ELEMENT_ID = "app";

export const ROUTES = {
  home: "/",
  contacts: "/contacts",
};

export const LOGO_COLOR = new Color(0xf72585);

export const YA_METRICA_COUNTER_ID = 88787331;

export enum MetricaTarget {
  Github = "github",
  LinkedIn = "linked-in",
  HabrCareer = "habr-career",
}
