/**
 * Inital state
 */

const initialState = {
  isAuthenticated: false,
};

/**
 * Expo
 */

export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
